- `conda create -n blackboxProject python=3.10`  (preferibile la 3.10.12)
- `conda activate blackboxProject`
  - Eventualmente: `python -m pip install carla==0.9.16`
- `python -m pip install ipykernel`
- `python -m ipykernel install --user --name blackboxProject --display-name "CARLA 0.9.16 - Python 3.10"`

